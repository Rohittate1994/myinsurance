import { CustPipe2Pipe } from './cust-pipe2.pipe';

describe('CustPipe2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustPipe2Pipe();
    expect(pipe).toBeTruthy();
  });
});
